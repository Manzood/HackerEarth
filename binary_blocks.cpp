#include "bits/stdc++.h"
using namespace std;
#define debug(x) cout << #x << " = " << x << endl;
#define int long long

int32_t main () {
    int n, m;
    scanf("%lld%lld", &n, &m);
    int new_n = n;
    if (n % k != 0) new_n = n + (k - n % k);
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < m; j++) {

        }
    }
}
